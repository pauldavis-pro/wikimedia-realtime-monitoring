const constants = Object.freeze({
	RECENT_CHANGE_URI: "https://stream.wikimedia.org/v2/stream/recentchange",
	THROTTLING_TIME_IN_MS: 2000, // 2s
	DATA_SLICE_LIMIT: 100,
});

export default constants;
